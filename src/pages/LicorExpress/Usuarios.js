import { Typography } from '@mui/material';

import MainCard from 'components/MainCard';

const UsuariosPage = () => (
  <MainCard title="Aqui van todos los usuarios creados, solo lo puede ver el admin mlp">
    <Typography variant="body2">
      TODO ESTA VACIO EN ESA MONDA
    </Typography>
  </MainCard>
);

export default UsuariosPage;
